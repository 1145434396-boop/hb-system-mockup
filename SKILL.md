---
name: hb-system-mockup
description: 生成伙伴云系统界面示意图（1640 宽 HTML 源文件＋2x PNG）。当用户要"画界面示意图""画个系统界面/截图效果""界面 mockup""给报告/方案配系统图""画个列表页/详情页/工作台/看板效果图"，或 hb-digital-proposal 报告给出图需求单需要配图时，必须使用本 skill。两种产出按用途分：营销类（底层界面＋浮层强调说明，用于报告和宣传）、产品设计类（纯界面图，用户可照着在伙伴云里搭建）。画面忠于伙伴云真实产品组件，不自造组件。不用于：海报/朋友圈营销图、流程图（hb-flowchart）、ER 图（hb-er-draw）、网站（hb-website-creator）。
---

# 伙伴云系统界面示意图

产出"长得像伙伴云产品"的界面示意图。核心资产三层：**结构**（assets/c1～c3 三份结构文件，来自真实产品 DOM 实测，按需读）＋**骨架样式**（assets/base.css，尺寸布局，皮肤无关）＋**皮肤**（assets/skins/*.css，纯色彩 token）。拼装，不自由发挥。

## 工作流

### 1. 判输入，定样式

按 [references/skin-guide.md](references/skin-guide.md) 执行：

- 输入物**带样式**（带样式的 HTML、报告、界面设计需求、系统截图）→ 先问用户是否沿用原样式；沿用则提取色值做皮肤，要求重设计则推荐皮肤库
- 输入物**不带样式**（模糊需求、会议逐字稿）→ 直接推荐皮肤（给建议＋理由，不罗列清单，不暴露内部文件名）

### 2. 判用途，定画布

问一句"这图用来干什么"，两分：

- **营销类**（放进报告/给客户讲解/宣传）→ 底层窗口＋补充浮层。浮层只能展示底层尚未出现的组件、信息或结果态，禁止复制底层已有组件或相同信息。标注气泡不默认添加，只有用户明确要求业务价值标注时才使用。
- **产品设计类**（用户照着搭建）→ 仅底层，就是一张"产品截图"，画面里每个元素都必须能在伙伴云里搭出来。

画布尺寸、浮层规则、验证与导出细节见 [references/canvas-spec.md](references/canvas-spec.md)。

### 3. 确认图需求单

动工前和用户对齐（来自 hb-digital-proposal 的调用也是这个格式）：

- 页面类型：列表页（网格/卡片/看板/甘特/日历/任务/透视）、详情页（默认自定义详情页）、记录详情弹窗、标准表单编辑态、创建表单、工作台、数据分析页、企业门户
- 要呈现的字段和数据（用用户业务的真实字段名，数据编得像真的：编号有规则、金额有零头、人名像人名）
- 营销类另加：浮层要突出什么

多张图一起做时列个清单让用户确认一次，不逐张打断。

### 4. 拼装

1. 结构文件三份，**按页面类型按需读，禁止三份全读**：

   | 文件                                                     | 内容                                                                                                                            | 什么时候读                                                               |
   | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
   | [assets/c1-shell.html](assets/c1-shell.html)             | 产品壳、一级顶栏、左侧导航的架构与组件目录（无业务示例）                                                                        | 仅画列表页、工作台、门户或用户明确要求产品壳时读取；独立自定义详情页不读 |
   | [assets/c2-table-form.html](assets/c2-table-form.html)   | 视图页签、工具栏、数据表格、字段、标准表单编辑态、创建表单、卡片视图、业务视图的架构与组件目录（无业务示例、无默认顺序）        | 画列表页、创建/编辑态表单、任何带字段的图                                |
   | [assets/c3-page-detail.html](assets/c3-page-detail.html) | 独立自定义详情页、记录详情弹窗、工作台/数据分析部件、企业门户、内容组件、原生部件的实测架构与组件目录（无业务示例、无默认顺序） | 画详情页、详情弹窗、工作台、看板页、门户                                 |

   详情形态按以下规则路由：

   - 用户只说“详情页/详情界面”时，默认读取 c3，使用“独立自定义详情页”；带字段时再读取 c2 第六区。不读取 c1，不套产品壳与弹窗遮罩；**顶部记录功能区默认包含**（翻页＋标题＋快捷按钮＋系统操作，实测产品自带）。
   - 只有用户明确要求展示编辑态、标准表单编辑或字段录入控件时，才读取 c1＋c2，使用“标准表单编辑态”。
   - 用户明确要求弹窗详情时，读取 c1＋c3，使用“记录详情弹窗”；其中 widget 卡字段结构需要再读 c2 第六区。
   - 纯工作台图只读 c1＋c3。
     c1～c3 中的 `<template>` 都是独立架构或组件，**不是整页范例**。根据 `data-architecture`、`data-component` 和结构注释识别用途，只选需求需要的部分，不按文件顺序整段复制。c1 的 `.main` 是页面内容插槽，把 c2/c3 的页面内部结构放进去，不再嵌套第二个 `.main`。

2. 单文件 HTML，用 cat 拼接，别手抄进上下文：`<style>`＝皮肤 css＋base.css＋本图补充样式；`<body>` 开头拼 [assets/icons.svg](assets/icons.svg)（图标雪碧图），末尾拼 fit.js。手写的只有本图补充样式和 `.stage` 内容。**改过 base.css/皮肤后必须重拼已有图**，图里嵌的是拼装时的快照

3. **硬规则：只用结构文件里的组件**。要画名录（18 类 70 项）里有但文件未收录形态的部件，先告诉用户该组件还没实测采集，经确认后按注释里的实测尺寸就近仿写；名录里没有的东西不画
   3a. **图标一律 `<svg class="ico"><use href="#i-名称"/></svg>`**，63 个名称见 icons.svg；着色用 `ic-*`/`tone-*` 工具类，不写行内色。缺的图标先补进 icons.svg 再用，不内联 path
   3b. 内容数据按 [references/principles/anti-sameness.md](references/principles/anti-sameness.md) 编：带零头、有非理想态、行数不取整、同批图版式错开

4. 图表部件的绘图区用内联 SVG 手绘（折线/柱/饼等），图表类型按 [references/principles/dashboard-chart-selection.md](references/principles/dashboard-chart-selection.md) 选，别选错。**SVG 里禁止写死色值**：主系列 `var(--primary)`（同系第二层加 `opacity=".45"`）、状态色 `var(--c-green/red/orange/blue/purple)`、轴线 `var(--line)`、轴标字 `var(--ink-45)`。写死十六进制的后果是换皮肤时图表不跟着变，整张图配色崩掉。图例色块用 `<rect>` 着色，别用 ■ 字符（字符是文字色，着不上）

5. 状态标签用红绿灯语义色，按钮分主/次/警示/置灰，表单列数一致：这些自检判据在 [references/principles/](references/principles/README.md)，拼完对照过一遍

6. 自定义详情页的“标题卡片”只放标题、副标题/编号和关键字段，**禁止放按钮**。操作按钮的唯一容身处是顶部记录功能区（快捷按钮三态：实底/线框/置灰）

### 5. 验证与交付

1. `python3 scripts/check.py 图.html`：检查写死色值、自造组件、缺失图标、导出前置，并渲染一遍量空隙（组件之间、组件到画布边缘留大片空白会报 empty-gap；组件内部的留白由数据量决定，不查）。Blocker 必须清零才能继续，empty-gap 逐条处理：补内容或收紧布局
2. 浏览器渲染核对（高度贴合、无溢出、组件不走样）→ 回填 `.stage` 高度
3. 出 2x PNG。交付＝HTML 源文件＋PNG；嵌报告场景另按 canvas-spec 的 SVG 管线导出

产出物落点：**PNG 在外、HTML 进 `源文件/` 子文件夹**。独立出图放当前工作目录（`图名@2x.png` 在外，`源文件/图名.html`）；hb-digital-proposal 报告配图放报告项目的 `figures/` 目录（PNG 在 figures/ 下，HTML 在 `figures/源文件/` 下）。PNG 是给人看和嵌报告的，HTML 是可再导出的源稿，分开放免得交付目录混杂。渲染中间产物（preview 截图）放 scratchpad，不留在交付目录。

## 硬约束

- **不自造组件**：伙伴云没有的控件、布局、交互形态一律不画。拿不准某组件长什么样时，宁可问用户或用已实测组件替代，不要凭想象画
- **结构与皮肤分离**：改色只动皮肤 token，不改结构和骨架样式里的尺寸
- **尺寸与层次是实测的**：顶栏 56、侧栏 248、行高 35、标签 20、按钮 32/24 这些值来自真实产品，改了就不像了。组件与背景的层次关系同理——是"白卡浮在灰底上"还是"透明融进容器"，以结构文件的 data-measured 注释为准；没有注释的组件先去产品实测再画，不许猜（猜错过四次：工具栏透灰底、看板列臆造灰底、筛选部件融背景、底部合计堆成左下角一行文字而不是与列对齐的 tfoot）
- **页头卡不放按钮**：标题卡片是信息摘要，不是操作区；按钮只能放在记录操作栏或其他已收录的操作容器
- **自定义详情页必须独立存在**：默认交付＝记录功能区＋页面内容画布；禁止套记录弹窗、一级顶栏、左侧导航或底部栏。只有用户明确要求产品壳/弹窗时才调用对应架构
- **营销浮层不得重复**：浮层必须补充底层没有的真实组件或信息；禁止把底层卡片、字段、统计或表格再复制放大一遍。浮层不得遮住关键内容，也不得通过裁短底层窗口造成页面底部缺失
- **Skill 资产是唯一结构真相源**：新实采的界面结构直接沉淀到 `assets/c1-shell.html`、`assets/c2-table-form.html`、`assets/c3-page-detail.html` 和必要的 `assets/base.css`；
- 示意图内容只写业务结论，不留设计过程的痕迹
