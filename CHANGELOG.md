# 更新日志

## v0.2.2 — 2026-08-21

- **内置 Chrome 渲染环境（零下载）**：`assets/chrome/` 内置 chrome-headless-shell 152.0.7977.54（Linux x64）分卷 chs_vol_aa/ab（单文件 <100MB），skill 走到哪渲染环境到哪，不依赖资源库。check.py 的 Chrome 路径从写死 macOS 改为自动探测（CHROME_BIN → 本机 Chrome → 已解压目录 → PATH → 内置分卷自动解压到 ~），新增 `references/chrome-env.md` 记录引导顺序与校验和，canvas-spec 导出命令同步改用探测到的路径。huoban-solution-report 的 make_pdf.py 共用本 skill 的分卷

## v0.2.1 — 2026-08-19

- **新增 Medium `column-uneven` 检查**：量同一栅格行里各栏的底部落差，超过 24px 报警。这块空白落在组件之间，empty-gap 的 SKIP 名单管不着，目检又容易被「左右各自都很整齐」的错觉盖过去
- **页签内直接放字段的容器规则**：`.page-tabs-body > .field` 补左右 16、行间 12、首行留顶；缺这条时一排灰色值框糊成一片，看不出「白卡片＋灰值框」的层次
- canvas-spec 自检清单同步补入并排栏高度核对

## v0.2.0 — 2026-08-10

一轮全量回产品实测的校正版：骨架照实改、层次关系补齐、检查从纯静态升级为"静态＋渲染探测"。

### 骨架校正（回产品实测，2026-08-10）

- **产品壳改为左右两列、左列通顶**：左侧导航 248 宽从顶贯到底（顶部 56 高工作区块），一级顶栏只覆盖右侧内容区。官方四种布局做成 `.window` 主题类：`theme-band`（顶部整条同色，默认）/ `theme-side` / `theme-full` / `theme-light`
- **底部合计＝表格 tfoot**：与列一一对齐、最左格记录数、配了统计的列写"统计方式＋数值"，未配置的留空；废除堆在左下角的一行说明文字
- **详情页骨架修复**：记录功能区（56）与画布 min-height:100% 平级直铺时恒比窗口高 56、底部被裁——补 `.item-page-toolbar + .item-page-canvas { min-height: calc(100% - 56px) }`，`.custom-detail-page` 改 flex 列
- 工具栏 48 高＋下边线；单元格右竖分隔线（表头无）；首列勾选＋行号合并 95 宽；行高 35 为矮档（另有中/高档，高档 72 可换行）
- 看板：列间距 24、列头下 2px 彩色实线（`line-*`）、卡片三层描边影、卡片字段竖排（名 12/值 14）
- 日历：星期表头 25 高左对齐、日期格 86 高、本月数字主色/非本月浅灰、今天主色实底
- 视图区白卡容器（.view-box）、筛选部件白卡、看板列透明：层次关系以 data-measured 注释为准
- 新增组件：分组行、分页器、甘特时间粒度切换组
- 选项步骤条：切口 10px、已过段主色 25%；字段标签左对齐 8、分组标题居中粗体、长文本跨行无灰底

### 检查升级（scripts/check.py）

- 新增渲染探测（headless Chrome，注入测量脚本＋dump-dom 取回）：
  - **High `content-clipped`**：窗口内容比窗口高、底部被裁（整图观感偏下）
  - **High `float-out`**：浮层探出画布
  - **Medium `empty-gap`**：布局层大片空白（组件之间、组件到画布边缘）；组件内部留白由数据量决定，不查
- `--no-render` 跳过渲染只跑文本检查；无 Chrome 自动降级

### 画布与产出

- 画布全局透明＋无阴影（PNG 导出加 `--default-background-color=00000000`）；细边替代投影
- 浮层左右两侧都可探出（`float-left`），同批图错开位置；左探出两条纪律（露出导航关键项；详情页不用左探出）
- 产出物分类落点：PNG 在外、HTML 进 `源文件/` 子文件夹
- `.stage` 高度回填口诀与"先填大再量会虚高"的坑写入 canvas-spec

### 皮肤

- 新增北欧冷灰（Nord）、柔和粉彩（Catppuccin Latte），共 10 套
- Radix 12 级取色尺入 skin-guide；`--backdrop` 转备用 token
- 壳层主题×皮肤的配色关系写入 skin-guide


## v0.1.0 — 2026-08-09

首个可用版本。产出"长得像伙伴云产品"的系统界面示意图：单文件 HTML 源 ＋ 2x PNG。

### 核心决策

- **结构与皮肤分离**。结构来自真实产品 DOM 实测，骨架样式只管尺寸布局，颜色全部走 CSS 变量由皮肤提供。换配色只动 token，不碰结构。
- **不自造组件**。伙伴云没有的控件、布局、交互形态一律不画；名录里有但未实测的形态，先告知用户再按实测尺寸仿写。
- **两种产出按用途分**。产品设计类＝纯界面图，撑满窗口、可照着在平台里搭建；营销类＝底层界面＋浮层补充说明，用于报告和讲解。

### 资产

**结构**（真实产品 DOM 实测，按页面类型按需读，禁止全读）
- `assets/c1-shell.html` — 产品壳、一级顶栏、左侧导航
- `assets/c2-table-form.html` — 视图页签、工具栏、数据表格、字段、表单、卡片视图、七种业务视图
- `assets/c3-page-detail.html` — 独立详情页、记录详情弹窗、工作台与数据分析部件、企业门户、内容组件

**样式**
- `assets/base.css` — 骨架样式。尺寸取自实测（顶栏 56、侧栏 248、行高 35、标签 20、按钮 32/24）
- `assets/skins/` — 10 套皮肤：产品原生蓝、藏蓝金、科技暗黑、温暖人文、高端黑金、活力紫、中式雅韵、安全红、北欧冷灰（Nord 色系）、柔和粉彩（Catppuccin Latte 色系）
- `assets/icons.svg` — 63 个图标的 symbol 雪碧图，线性与实色两类。做成静态 symbol 而非脚本注入，因为 SVG foreignObject 导出不执行脚本
- `assets/fit.js` — 画布自适应。全屏模式贴合窗口宽，画布模式只缩不放

**工具**
- `scripts/check.py` — 静态检查，零依赖。分级报告写死色值（Blocker）、自造组件（High）、本图独有类（Nit）、SVG 导出前置条件

**规范**
- `references/canvas-spec.md` — 画布规范与两条导出管线（2x PNG、SVG 嵌报告）
- `references/skin-guide.md` — 输入判定、皮肤选型、从品牌色造皮肤、Radix 12 级取色尺
- `references/principles/` — 8 份设计判据：视觉四原则、布局、配色、组件规范、交互三原则、看板数据故事、图表选型、防千篇一律

### 与其他 skill 的协作

- **huoban-solution-report**：报告皮肤与本库同名同气质。报告选定皮肤后，配图用同名皮肤，报告与图自动同色系。报告通过图需求单调用本 skill 出图，图落在报告项目的 `figures/`。

### 已知边界

- 长尾部件（内容组件、日历、看板、甘特）的骨架样式只有要点版，用到时按结构文件注释里的实测尺寸就地补写。
- 图表绘图区靠内联 SVG 手绘，颜色必须用 token；已在 `check.py` 里设为 Blocker 拦截。
- 尚无页面骨架模板（skeletons），常见页面仍从组件现拼。
- 尚无皮肤 showcase 图，选皮肤靠文字描述。

### 待办

- 页面骨架模板：常见页面从零拼改为填空
- 皮肤 showcase：让用户看图选而不是听描述
- 对比度校验并入 `check.py`
- 品牌色生成器：一个 hex 自动生成亮暗整套皮肤
